# Workflow YAML Spec

Workflow YAML contains graph metadata, UI hints, phases, nodes and edges. Definitions are loaded into the database and executed by Temporal-backed enterprise workflows with agent/model/tool/sandbox evidence persisted in the event ledger.
