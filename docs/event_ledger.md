# Event Ledger

Events are append-only records. Important run, phase, node, agent, tool, file, artifact, requirement, review, test, quality, approval, feedback, learning and batch actions are persisted.
